# Contexto Tech

## Como rodar

1. Crie um venv com `python3 -m venv`
2. Ative com `source venv/bin/activate`
3. Instale os requisitos com `pip install -r requirements.txt`
4. Instalar o modelo do spaCy com `python3 -m spacy download pt_core_news_md`
5. Execute o site com `python3 app.py`